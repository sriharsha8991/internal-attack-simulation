# Copy to .env and fill in real values. Loaded by your shell or a tool like
# `direnv` / `dotenv-cli`. The orchestrator itself does NOT auto-load .env.
BAS_BASE_URL=http://pelorushub.online:31763
BAS_DRY_RUN=false

# Optional environment pin (leave unset to use most-recent).
# BAS_ENVIRONMENT_ID=
# BAS_ENVIRONMENT_NAME=

GEMINI_API_KEY=
LOG_LEVEL=INFO

# LangSmith / LangGraph Studio (optional — needed for `langgraph dev`).
# Free signup: https://smith.langchain.com/settings
LANGSMITH_API_KEY=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
